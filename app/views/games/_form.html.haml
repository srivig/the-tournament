= simple_form_for @game, html: {class: 'form-horizontal'} do |f|
  = f.simple_fields_for :game_records do |r|
    = r.association :player, disabled: true
    = r.input :score

  .form-group
    .col-sm-6.col-sm-offset-2
      = f.button :submit, class: 'btn-danger'
      -# = link_to t('.cancel', default: t("helpers.links.cancel")), tournament_games_path(@game.tournament)
