= content_for :page_title, "#{@tournament.title} #{t('tournament_menu.basic_info')}"
= content_for :keywords, @tournament.title

= render partial: "shared/tournament_info", locals: {tournament:@tournament, active:4}

%section
  %h1.headline
    = fa_icon("cog")
    = t('tournament_menu.basic_info')
    -# SP版: ボタングループでメニュー表示
    = render partial: "shared/sp_tournament_menu", locals: {tournament:@tournament} 
  = render :partial => "form"
