= content_for :page_title, t(".create_new_tournament")

=link_to sanitize('<i class="fa fa-reply"></i> ') + t('back_to_home'), root_path, class: 'btn btn-default'

%section
  %h1.title.clearfix
    = t('.create_new_tournament')

%section
  %h1.headline
    %i.fa.fa-cog
    = t('tournament_menu.basic_info')
  = render :partial => "form"
